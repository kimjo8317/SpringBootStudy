인텔리제이포트확인 = run -> Edit configurations

엔티티(entity)=
데이터베이스 테이블과 매핑되는 자바클래스
모델 또는 도메인모델 이라고도 부른다

리파지토리(Repository)=
엔티티만으로는 db데이터를 저장 조회불가능 ->
실제db와 연동하는 JPA리포지터리,
findall/save 등 테이블에 접근하는 메서드를 사용하기 위한 "인터페이스"
(CRUD)를 어떻게 처리할지 정희하는것이 리포지토리

